{
  "mcpServers": {
    "hostinger-api": {
      "command": "cmd",
      "args": ["/c", "hostinger-api-mcp"],
      "env": {
        "DEBUG": "false",
        "API_TOKEN": "${HOSTINGER_API_TOKEN}"
      }
    }
  }
}
