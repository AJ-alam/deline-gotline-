Django==5.2.13
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
django-filter==25.2
drf-spectacular==0.29.0
python-decouple==3.8
Pillow==11.3.0
gunicorn==25.3.0
psycopg2-binary==2.9.11
whitenoise==6.12.0
dj-database-url==3.1.2
python-dotenv==1.0.1
supabase==2.15.3
reportlab==4.3.1
openpyxl==3.1.5
# Reads a generated PDF back in the tests, so the approval letter is checked by
# what a reader actually gets out of it rather than by grepping the bytes — the
# fonts are embedded as subsets, so a byte search finds nothing in a correct
# document.
pypdf==6.1.1

# Fernet, which encrypts every stored SIN and set of bank details. It arrives
# transitively today (supabase pulls it in) and is pinned here because
# `funding.services.identifiers` imports it directly: a transitive dependency
# dropping it would break storing a government identifier, at runtime, on a
# form somebody was filling in.
cryptography==50.0.0
